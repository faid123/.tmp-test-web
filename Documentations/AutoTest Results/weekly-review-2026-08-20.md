# Weekly Documentation Review -- 2026-08-20

Branch: `nyunt/dev-W7.1` @ `4d7438b`

## Live-branch alignment

_Alignment with the live branch could not be checked (no remote access)._

## Test suite

13 suites / 221 tests -> 19 suites / 582 tests

## Dependency audit (npm audit)

{"info":0,"low":0,"moderate":3,"high":6,"critical":1,"total":10} -> {"info":0,"low":0,"moderate":3,"high":6,"critical":1,"total":10}

## Commits since the last review

```
4d7438b docs: sync Documentations/ from dev-deploy-documentation, retarget at nyunt/dev-W7.1
f665d4d update to fix the occlusion heatmap toggle button to be beside undercut heatmap
9950df7 jaw occlusion dll function implemented
f117b5d implemented load proposed design from DLL for metal and acrylic rpd 2d design
411c786 Deploy 6107a903419f9d7d2dcbe53948e6d091270013d4 (run 35)
53f6a29 Deploy 11114d8904ab57f6aec716707084de25f7d9732d (run 34)
af5de0a Deploy c5fcfec8ef977bf1a587649c78a092ab18e0e278 (run 33)
b2fd6ce Deploy 7cd042318ad8e549357962d380d3020097851d2c (run 31)
52fd813 Deploy f5076ef20ea8e3e74491610329ff584b22ac72f5 (run 30)
f24d27a Deploy ae36fb58a11e6a396e129ef49d592305eddea2fd (run 29)
a2beac5 Deploy 2970c0b863f1955e0be71d62cd27919ea0a6f230 (run 28)
bdbfd1e Deploy 4b622f3cda1d32e3bb1d076816721ef4cb41e8bb (run 27)
0981697 Deploy 218ab45275d153d0769b6f8f960513d1d4192468 (run 26)
6870626 Deploy 4f034f6aa72bb4cd093b0c8e306cf04defd6be27 (run 25)
05193d1 Deploy a81abed9f31e6a77b2b095c6ee8fc70ec0d8f247 (run 24)
9a69afb Deploy d5d5121f12287c45b91d7a0d62acf017d6d98c64 (run 23)
1850ef4 Deploy bf0b89fb1525a1931d76d8085167b82955903f84 (run 22)
f5f54bf Deploy d1bc59921e9ccc47562d6fb1b6786058108d98b7 (run 21)
cf31bb5 Deploy 243f764a8643682539c49fa49a6c76ec902e267c (run 20)
72451ef Deploy e1acfe4e63d5741f7fd996ce9419c8611c45a905 (run 19)
1ade0f7 Deploy b19352aec25dc175cd0e200257bfedbac4304081 (run 18)
4b6c819 Deploy 3b86f5591ab45679d8e11d4d6cc299a5b80bb7c7 (run 17)
a7e7f94 Deploy c906febd15751af6c5d2f0b6c131b77c9c289156 (run 16)
cb69eab Deploy e6211947568f3a814a5e815072586c201d653f07 (run 15)
1f9c075 Deploy bdd9a74cc83161c1429635ea3d79c343a9e5be9e (run 14)
a53681e Deploy d38ff71a6b34f1ff7945d886d471fdcada43b955 (run 13)
37af34f Deploy f70f127b0a5fc48a420fc9c130a79d92754f79bd (run 12)
b09d580 Deploy 5b2ad409a698a0dc7d0ffc47e6be119af2772d90 (run 11)
bb856d1 Deploy 889ab18036dc137e183354bef6626c47baa1692f (run 10)
```

## Docs likely needing a manual/AI-assisted update

- [ ] **Documentations/DEPLOY_DOC_REVIEW.md**
  - .github/workflows/deploy-review.yml (changes the weekly-doc-review process itself -- re-read DEPLOY_DOC_REVIEW.md to keep its description of the process in sync)
  - tools/weekly-doc-review.mjs (changes the weekly-doc-review process itself -- re-read DEPLOY_DOC_REVIEW.md to keep its description of the process in sync)

- [ ] **How to Setup.docx**
  - Documentations/setup/02-branching-and-collaboration.md (How to Setup.docx is regenerated from Documentations/setup/*.md and mirrors deploy.yml's pipeline steps -- keep it in sync with whichever changed)

- [ ] **Automated_Test_Suite_Report.docx**
  - __tests__/JawDesign.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/JawDesignProposal.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/assemblies.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/caseComment.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/caseEmailBody.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/caseEnrichment.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/dashboard.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/fixtures/case_2001_getall.json (changes the test suite this document reports counts/breakdown for)
  - __tests__/helpBot.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/helpers/appSources.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/helpers/jawStructIO.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/helpers/teeth.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/imageFiles.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/importResolution.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/jawStruct.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/jawStructLingualPlate.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/jawStructMajorSwitch.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/jawStructRoundTrip.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/mergeInstructions.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/pageTour.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/passwordReset.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/placementRules.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/reciprocatingExclusivity.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/stress-test.js (changes the test suite this document reports counts/breakdown for)
  - __tests__/userSuggest.test.mjs (changes the test suite this document reports counts/breakdown for)
  - package.json (changes the test suite this document reports counts/breakdown for)

- [ ] **UAT_Report.docx (§2)**
  - __tests__/JawDesign.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/JawDesignProposal.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/assemblies.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/caseComment.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/caseEmailBody.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/caseEnrichment.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/dashboard.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/fixtures/case_2001_getall.json (changes the test suite this document reports counts/breakdown for)
  - __tests__/helpBot.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/helpers/appSources.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/helpers/jawStructIO.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/helpers/teeth.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/imageFiles.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/importResolution.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/jawStruct.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/jawStructLingualPlate.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/jawStructMajorSwitch.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/jawStructRoundTrip.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/mergeInstructions.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/pageTour.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/passwordReset.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/placementRules.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/reciprocatingExclusivity.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/stress-test.js (changes the test suite this document reports counts/breakdown for)
  - __tests__/userSuggest.test.mjs (changes the test suite this document reports counts/breakdown for)
  - package.json (changes the test suite this document reports counts/breakdown for)

- [ ] **SmartRPD_Documentation.docx (§4, §11)**
  - __tests__/JawDesign.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/JawDesignProposal.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/assemblies.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/caseComment.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/caseEmailBody.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/caseEnrichment.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/dashboard.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/fixtures/case_2001_getall.json (changes the test suite this document reports counts/breakdown for)
  - __tests__/helpBot.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/helpers/appSources.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/helpers/jawStructIO.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/helpers/teeth.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/imageFiles.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/importResolution.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/jawStruct.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/jawStructLingualPlate.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/jawStructMajorSwitch.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/jawStructRoundTrip.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/mergeInstructions.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/pageTour.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/passwordReset.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/placementRules.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/reciprocatingExclusivity.test.mjs (changes the test suite this document reports counts/breakdown for)
  - __tests__/stress-test.js (changes the test suite this document reports counts/breakdown for)
  - __tests__/userSuggest.test.mjs (changes the test suite this document reports counts/breakdown for)
  - package.json (changes the test suite this document reports counts/breakdown for)

- [ ] **Feedback_Triage_Report.docx (new TSK candidate?)**
  - css/2Dannotation.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - css/admin/admin_machineid.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - css/admin/admin_users.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - css/app-shell.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - css/case_list.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - css/changePassword.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - css/chat.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - css/createCase.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - css/dashboard.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - css/helpBot.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - css/login.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - css/noticeboard.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - css/notification.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - css/pageTour.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - css/style.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - css/toast.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - css/tokens.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/.DS_Store (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/.DS_Store (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/.DS_Store (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/2DAnnotation.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/JawDesign.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/JawDesignProposal.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/annotationCatalog.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/annotationLocks.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/annotationPlacement.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/annotationRender.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/annotationTeethModel.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/annotationVisuals.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/caseNote.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/clinicalInfo.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/components.bar.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/components.clasp.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/components.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/components.major.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/components.mesh.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/components.minor.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/components.plate.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/components.rest.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/constants.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/dotnetBinaryFormatter.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/instructionEditor.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/jawStructApi.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/jawStructApply.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/jawStructCodec.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/jawStructCodes.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/mergeInstructions.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/noticeboard.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/preview3D.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/preview3DApproval.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/preview3DSurvey.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/previewTabs.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/referenceImages.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/toothUtils.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/pages/AnnotationHistory.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/pages/caseManagement.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/pages/createCase.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/pages/dashboard.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/pages/notifications.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/pages/versionHistory.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/pages/viewerShell.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/accessibility.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/api.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/apiLog.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/appSidebar.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/caseEnrichment.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/caseLinks.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/caseRoles.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/changePassword.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/chat.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/helpBot.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/helpMatcher.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/helpTopics.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/imageFiles.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/pageContext.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/pageTour.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/timestamps.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/toast.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/tourSteps.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/userSuggest.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/vertexMatch.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/pages/2DAnnotation.html (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/pages/AnnotationHistory.html (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/pages/ThreeDViewer.html (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/pages/ThreeDViewer.tourprobe.html (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/pages/VersionHistory.html (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/pages/admin/admin_case_list.html (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/pages/admin/admin_machineid.html (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/pages/admin/admin_users.html (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/viewer3d/OFFLoader.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/viewer3d/STLMeshLoader.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/viewer3d/artificialTeeth.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/viewer3d/index.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/viewer3d/newControls.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/viewer3d/resetButton.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/viewer3d/vertexColors.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)

- [ ] **Traceability_Document.docx**
  - css/2Dannotation.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - css/admin/admin_machineid.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - css/admin/admin_users.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - css/app-shell.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - css/case_list.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - css/changePassword.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - css/chat.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - css/createCase.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - css/dashboard.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - css/helpBot.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - css/login.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - css/noticeboard.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - css/notification.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - css/pageTour.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - css/style.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - css/toast.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - css/tokens.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/.DS_Store (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/.DS_Store (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/.DS_Store (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/2DAnnotation.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/JawDesign.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/JawDesignProposal.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/annotationCatalog.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/annotationLocks.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/annotationPlacement.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/annotationRender.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/annotationTeethModel.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/annotationVisuals.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/caseNote.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/clinicalInfo.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/components.bar.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/components.clasp.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/components.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/components.major.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/components.mesh.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/components.minor.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/components.plate.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/components.rest.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/constants.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/dotnetBinaryFormatter.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/instructionEditor.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/jawStructApi.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/jawStructApply.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/jawStructCodec.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/jawStructCodes.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/mergeInstructions.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/noticeboard.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/preview3D.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/preview3DApproval.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/preview3DSurvey.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/previewTabs.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/referenceImages.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/toothUtils.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/pages/AnnotationHistory.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/pages/caseManagement.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/pages/createCase.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/pages/dashboard.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/pages/notifications.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/pages/versionHistory.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/pages/viewerShell.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/accessibility.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/api.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/apiLog.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/appSidebar.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/caseEnrichment.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/caseLinks.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/caseRoles.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/changePassword.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/chat.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/helpBot.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/helpMatcher.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/helpTopics.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/imageFiles.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/pageContext.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/pageTour.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/timestamps.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/toast.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/tourSteps.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/userSuggest.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/vertexMatch.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/pages/2DAnnotation.html (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/pages/AnnotationHistory.html (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/pages/ThreeDViewer.html (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/pages/ThreeDViewer.tourprobe.html (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/pages/VersionHistory.html (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/pages/admin/admin_case_list.html (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/pages/admin/admin_machineid.html (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/pages/admin/admin_users.html (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/viewer3d/OFFLoader.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/viewer3d/STLMeshLoader.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/viewer3d/artificialTeeth.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/viewer3d/index.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/viewer3d/newControls.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/viewer3d/resetButton.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/viewer3d/vertexColors.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)

- [ ] **SmartRPD_Documentation.docx**
  - css/2Dannotation.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - css/admin/admin_machineid.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - css/admin/admin_users.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - css/app-shell.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - css/case_list.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - css/changePassword.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - css/chat.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - css/createCase.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - css/dashboard.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - css/helpBot.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - css/login.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - css/noticeboard.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - css/notification.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - css/pageTour.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - css/style.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - css/toast.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - css/tokens.css (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/.DS_Store (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/.DS_Store (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/.DS_Store (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/2DAnnotation.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/JawDesign.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/JawDesignProposal.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/annotationCatalog.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/annotationLocks.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/annotationPlacement.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/annotationRender.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/annotationTeethModel.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/annotationVisuals.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/caseNote.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/clinicalInfo.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/components.bar.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/components.clasp.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/components.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/components.major.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/components.mesh.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/components.minor.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/components.plate.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/components.rest.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/constants.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/dotnetBinaryFormatter.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/instructionEditor.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/jawStructApi.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/jawStructApply.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/jawStructCodec.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/jawStructCodes.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/mergeInstructions.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/noticeboard.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/preview3D.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/preview3DApproval.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/preview3DSurvey.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/previewTabs.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/referenceImages.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/2D/toothUtils.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/pages/AnnotationHistory.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/pages/caseManagement.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/pages/createCase.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/pages/dashboard.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/pages/notifications.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/pages/versionHistory.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/pages/viewerShell.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/accessibility.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/api.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/apiLog.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/appSidebar.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/caseEnrichment.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/caseLinks.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/caseRoles.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/changePassword.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/chat.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/helpBot.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/helpMatcher.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/helpTopics.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/imageFiles.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/pageContext.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/pageTour.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/timestamps.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/toast.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/tourSteps.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/userSuggest.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/js/shared/vertexMatch.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/pages/2DAnnotation.html (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/pages/AnnotationHistory.html (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/pages/ThreeDViewer.html (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/pages/ThreeDViewer.tourprobe.html (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/pages/VersionHistory.html (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/pages/admin/admin_case_list.html (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/pages/admin/admin_machineid.html (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/pages/admin/admin_users.html (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/viewer3d/OFFLoader.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/viewer3d/STLMeshLoader.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/viewer3d/artificialTeeth.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/viewer3d/index.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/viewer3d/newControls.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/viewer3d/resetButton.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)
  - src/viewer3d/vertexColors.js (is a feature-code change not already covered above -- check whether it needs a new TSK row)

- [ ] **Cybersecurity_Protocol_Document.docx**
  - package-lock.json (touches an authentication, credential, dependency, or CDN-script surface this document audits)
  - src/js/admin/adminMachineId.js (touches an authentication, credential, dependency, or CDN-script surface this document audits)
  - src/js/admin/adminShell.js (touches an authentication, credential, dependency, or CDN-script surface this document audits)
  - src/js/admin/adminUsers.js (touches an authentication, credential, dependency, or CDN-script surface this document audits)
  - src/js/pages/login.js (touches an authentication, credential, dependency, or CDN-script surface this document audits)
  - src/js/shared/config.js (touches an authentication, credential, dependency, or CDN-script surface this document audits)
  - src/js/shared/crypt.js (touches an authentication, credential, dependency, or CDN-script surface this document audits)
  - src/js/shared/passwordReset.js (touches an authentication, credential, dependency, or CDN-script surface this document audits)
  - src/pages/case_list.html (touches an authentication, credential, dependency, or CDN-script surface this document audits)
  - src/viewer3d/ApiClient.js (touches an authentication, credential, dependency, or CDN-script surface this document audits)

## Raw data this run produced

- `Documentations/AutoTest Results/automated-test-run.json`
- `Documentations/AutoTest Results/automated-test-run-log.txt`
- `Documentations/AutoTest Results/npm-audit-run.json`
- `Documentations/AutoTest Results/npm-audit-run.txt`
- `Documentations/AutoTest Results/DOCS_TO_UPDATE.md` (same alignment status + checklist as this report, at a fixed filename)

See [Documentations/DEPLOY_DOC_REVIEW.md](../DEPLOY_DOC_REVIEW.md) for what to do with this report.
