%%{init: {'theme': 'base', 'themeVariables': { 'primaryColor': '#eaf6f3', 'primaryBorderColor': '#0d7d6c', 'primaryTextColor': '#16212b', 'lineColor': '#56626d', 'tertiaryColor': '#fdf8e3', 'fontFamily': 'Segoe UI, Helvetica, Arial, sans-serif', 'fontSize': '18px' }, 'flowchart': { 'curve': 'basis', 'nodeSpacing': 45, 'rankSpacing': 75, 'htmlLabels': true } } }%%
%% SmartRPD — Summary Application Flowchart
%% DRAFT — needs re-verification against a real git-archive extraction before this replaces the
%% published diagram (see chat, 2026-08-21 mermaid review). Not yet re-rendered to .png.
%% Paste into https://mermaid.live (or any Mermaid renderer) to view.

flowchart TD
    classDef screen fill:#eaf6f3,stroke:#0d7d6c,stroke-width:1.5px,color:#16212b;
    classDef pipe fill:#fdf8e3,stroke:#d69b3f,stroke-width:1px,color:#16212b;

    A(["Visitor"]) --> B["Login / Sign Up / Forgot Password (index.html)"]
    B -->|"Admin account"| C["Admin Console: Case List + Users + Machine IDs"]
    B -->|"Regular user"| D["Case List (search, filter, notifications)"]

    D --> E["Create Case (STL upload, invite collaborators)"]
    D --> F["Open an existing case"]
    E --> F

    F --> G["2D Annotation: RPD design workspace (tooth chart, components, embedded 3D preview, chat)"]
    F --> H["Case Dashboard (per-jaw pipeline status, captures)"]
    F --> I["3D Viewer (standalone)"]
    F --> J["Version History"]

    G -->|"Approve for lab"| K["Approval email to stakeholders"]
    G --> D
    H --> D
    I --> D
    J --> D

    C -->|"approve signups, manage users / machine IDs"| C
    C -.->|"open a case"| F

    D -.->|"available on most signed-in pages"| HelpBot["Help Bot — contextual AI assistant"]
    G -.->|"Load Proposed Design (Kennedy-classification-driven, DLL-backed)"| G

    subgraph PIPE["Case status pipeline"]
        direction LR
        L1["Draft"] --> L2["2D design (pending, drafted, approved)"] --> L3["3D design (pending, drafted, approved)"] --> L4["In production"] --> L5["Out for delivery"] --> L6["Delivered"] --> L7["Completed"]
    end

    class B,C,D,G,H,I,J screen;
    class L1,L2,L3,L4,L5,L6,L7 pipe;
